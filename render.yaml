services:
  - type: web
    name: ai-goal-predictor-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: "10000"
    plan: free
    autoDeploy: true
    region: oregon